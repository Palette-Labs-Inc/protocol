description: a <code>Buyer<code>'s search request
content:
  application/json:
    schema:
      type: object
      properties:
        message:
          type: object
          properties:
            intent:
              $ref: "#/components/schemas/SearchIntent"
      required:
        - message
