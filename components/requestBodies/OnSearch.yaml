description: Request body for a PSN's response to a <code>Buyer<code>'s search request
content:
  application/json:
    schema:
      type: object
      properties:
        message:
          type: object
          properties:
            intent:
              $ref: "#/components/schemas/ProviderList"
      required:
        - message
