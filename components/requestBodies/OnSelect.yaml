description: a <code>Buyer<code>'s order object
content:
  application/json:
    schema:
      type: object
      properties:
        message:
          type: object
          properties:
            order:
              $ref: "#/components/schemas/Cart"
      required:
        - message
