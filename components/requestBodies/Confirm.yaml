description: The <code>Buyer<code>'s information to initialize the Cart.
content:
  application/json:
    schema:
      type: object
      properties:
        message:
          type: object
          properties:
            cart_id:
              $ref: "#/components/schemas/Cart/properties/id"
            transaction_id: 
              $ref:  "#/components/schemas/PaymentTerm/properties/transaction_id"
      required:
        - message
