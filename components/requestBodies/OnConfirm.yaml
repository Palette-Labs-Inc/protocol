description: The newly created active order. Typically, this order would be in a NEW_ORDER status.
content:
  application/json:
    schema:
      type: object
      properties:
        action:
            enum:
                - ON_CONFIRM
        callback_reference: 
            $ref: '#/components/schemas/CallbackReferenceParameters'
        psn_parameters:
            $ref: '#/components/schemas/NodeParameters'
        message:
            type: object
            properties:
                order:
                    $ref: "#/components/schemas/Order"
            required:
                - order
      required:
        - action
        - message
        - callback_reference
        - psn_parameters
