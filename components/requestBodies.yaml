# search 
Search:
    $ref: './requestBodies/Search.yaml'

OnSearch:
    $ref: './requestBodies/OnSearch.yaml'

# select
Select:
    $ref: './requestBodies/Select.yaml'

OnSelect:
    $ref: './requestBodies/OnSelect.yaml'

# init
Init:
    $ref: './requestBodies/Init.yaml'

OnInit:
    $ref: './requestBodies/OnInit.yaml'
