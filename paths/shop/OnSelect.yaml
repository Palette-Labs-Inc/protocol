summary: On Select Callback
tags:
  - Buyer Supporting Node (BSN)
description: The <code>Buyer</code> selects items from the catalog
requestBody:
  $ref: '../../components/requestBodies/OnSelect.yaml'
responses:
  default: 
    $ref: '../../components/responses/BSNResponse.yaml'